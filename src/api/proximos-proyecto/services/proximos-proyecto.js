'use strict';

/**
 * proximos-proyecto service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::proximos-proyecto.proximos-proyecto');
